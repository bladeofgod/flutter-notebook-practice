import 'package:flutter/material.dart';

class AirPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(title: Text("air page"),),
      body: new Center(
        child: Text("air page"),
      ),
    );
  }

}